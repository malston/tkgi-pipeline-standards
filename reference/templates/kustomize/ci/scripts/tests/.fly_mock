#!/usr/bin/env bash
# This is a temporary mock for fly

# Log the command and arguments
echo "$@" > "/Users/markalston/git/tkgi-pipeline-standards/reference/templates/kustomize/ci/scripts/tests/.fly_last_command"

# Check if this is the command we're expecting
if [[ "$1" == "set-pipeline" ]]; then
  # Check if arguments match what we expect
  args="${@:2}"
  if [[ "$args" == *"-p main-test-foundation"* ]]; then
    exit 0
  fi
fi

# For testing other commands or argument patterns
exit 0
